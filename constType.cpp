#include <iostream>

int main() {
    const double Pi = 3.14; //cannot change a constant in c++
    //gives variable security for anything labeled constant
    //as a naming convention, we should capitalize every const 
    std::cout << Pi << "cm";
    return 0; //code ran succesfully without any errors
}
